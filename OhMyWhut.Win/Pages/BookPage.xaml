<Page
    x:Class="OhMyWhut.Win.Pages.BookPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OhMyWhut.Win.Pages"
    xmlns:vm="using:OhMyWhut.Win.ViewModels"
    xmlns:data="using:OhMyWhut.Win.Data"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.Resources>
        <DataTemplate x:Key="BookListViewTemplate"
                      x:DataType="data:Book">
            <Grid>
                <StackPanel Orientation="Vertical">
                    <TextBlock Text="{x:Bind Name}" />
                    <TextBlock Text="{x:Bind BorrowDate}" />
                    <TextBlock Text="{x:Bind ExpireDate}" />
                </StackPanel>
            </Grid>

        </DataTemplate>
    </Page.Resources>
    <Grid>
        <ListView x:Name="BookListView"
                  SelectionMode="Single"
                  ItemsSource="{x:Bind ViewModel.BookViewModel.BookList, Mode=TwoWay}"
                  ItemTemplate="{StaticResource BookListViewTemplate}" />
    </Grid>
</Page>
